---
title: "Polychoric Correlation"
author: "Rowan Carew"
date: "2023-12-13"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
###SAVE THIS SPACE FOR MERGING DATA SET AND DEFINING VARIABLES AS ORDINAL#####
```

```{r}
library(polycor)
poly_corr_car_ownership <- hetcor(database_rural[, c('p_mob_ptintensity', 'p_central_nonwien')], ML = TRUE)
print(poly_corr_car_ownership)

#sapply(lapply(database, unique), length)
########USE THIS TO DEBUG IN CASE THERE ARE NOT ENOUGH DISTINCT VARIABLES############
```

